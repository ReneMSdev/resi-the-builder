[pytest]
pythonpath = .
testpaths = tests
markers =
    live: hits the real Anthropic API (real token spend). Excluded by default.
    slow: slow test (e.g. LibreOffice PDF conversion). Excluded by default.
addopts = -m "not live and not slow"
